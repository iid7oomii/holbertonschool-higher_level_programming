-- script that creates a table called first_table with id INT name VARCHAR(256) description
-- use create table if not exists (id type, name type);
CREATE TABLE IF NOT EXISTS first_table (
id INT PRIMARY KEY ,
name VARCHAR(256)
);
